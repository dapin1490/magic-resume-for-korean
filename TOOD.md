## 미완료

- [ ] 신규 이력서 생성 시 기본 로케일 fallback을 `en`으로 정렬
  - `useResumeStore.createResume`의 `NEXT_LOCALE` 미존재 fallback이 `"zh"`로 되어 있어 `defaultLocale: "en"` 정책과 불일치
- [ ] JSON/PDF 가져오기 시 로케일에 맞는 초기 베이스 데이터 사용
  - 가져오기 과정에서 `initialResumeState` 고정 기반으로 병합되어 누락 필드에 중국어 기본값이 섞일 수 있음
- [ ] 백업 폴더를 변경했을 때, 이전에 사용했던 백업 폴더에 대한 접근 권한이 삭제되는지 확인할 것.

## 완료

- [x] 템플릿 미리보기(CreateResumeModal)에서 로케일에 맞는 초기 데이터 사용
  - `CreateResumeModal`의 미리보기 베이스를 `zh`/`en` 기준으로 분기해 한국어 환경에서 중국어 샘플이 노출되지 않도록 조정
  - 스냅샷/템플릿 미리보기 fallback도 `zh` 기본 대신 `en` 기본으로 정렬
- [x] 리치 텍스트 모듈과 이미지 그리드 모듈은 테마 색깔 적용이 안됨
  - 모듈 제목의 색상 및 하단 보더에 `globalSettings.themeColor`를 적용
- [x] 항목 리스트형 커스텀 섹션에 본문 내용이 없어도 줄이 비워져 부자연스러운 빈 공간이 생김
  - 본문 내용이 없으면 줄을 비워두지 않음
- [x] 커스텀 섹션에 day 입력 시 너무 빨리 제로 패딩이 적용됨: 사용자가 2자리 숫자를 입력하려고 해도 제로패딩 때문에 앞 자리 0을 수동으로 지워야 하는 문제
  - 사용자 입력 완료 후에 제로 패딩 적용함
- [x] LaTex 서식 지원 추가
  - `katex` 기반 렌더링 추가: `normalizeRichTextContent` 경로에서 `$...$`, `$$...$$` 구문 렌더링
  - 전역 스타일에 `katex/dist/katex.min.css` 로드
- [x] 메인 홈이 아닌 곳에서 언어를 변경했을 경우, 새로고침을 하지 않아도 UI가 바뀌거나, 알아서 새로고침해주기.
- [x] json에 저장되는 데이터 라벨이나 태그 중 중국어가 있는 부분을 영어로 바꾸기: 사용자가 작성한 '내용'은 그 언어 그대로 들어가야 하지만, 그 외에 내부적으로만 사용되는 부분은 영어로 바꾸기.
  - `initialResumeData`의 내부 라벨/태그 기본값(예: 섹션 제목, 기본 커스텀 라벨, 기본 제목)을 영어로 정리
  - 기존 백업 파일/기존 저장 데이터는 자동 마이그레이션하지 않고, 신규 생성분부터 영어 기본값 적용
- [x] resume 복제 기능 추가: 현재 생성된 resume을 복제하여 새로운 resume을 생성할 수 있게 함.
  - Workbench의 Preview Dock 복제 버튼에 더해 Dashboard 카드에서도 바로 복제 가능하도록 연결
  - 복제 시 새 resume ID 생성, 활성 resume 전환, 편집 화면으로 즉시 이동
- [x] 사용자가 로컬 폰트 로드하여 사용할 수 있게 하기
  - 폰트 파일 업로드(ttf, otf, woff, woff2) 및 Chrome Local Font Access API(`queryLocalFonts`)로 설치된 폰트 선택 지원
  - 로컬 폰트 사용 시 서버 PDF보내기 비활성화, 브라우저 프린트 안내
  - 번들 한국어 폰트 선택 시에는 PDF 버튼 유지, 브라우저 프린트 권장 문구만 표시
  - 미리보기: 내장 폰트 목록에 없는 폰트명을 기본 폰트로 덮어쓰지 않도록 `normalizeFontFamily` 동작 수정(설치 폰트 선택 후 미리보기 반영)
- [x] 한국어용 폰트 추가하기: 나눔고딕, 맑은고딕, Noto Sans KR 등
  - [x] export PDF 기능에도 폰트가 반영되어야 함: 반영은 성공했으나, 일부 폰트에서 시간초과 발생하기 때문에 한국어 사용자는 브라우저 프린트를 권장하도록 할 것
- [x] `YYYY/MM/DD` 형식 날짜 입력 지원
  - day 누락 시 `YYYY/MM`으로 작성되어야 함
  - 항상 연도 - 월 - 일 순서로 작성되어야 함
- [x] 기본 제공 섹션도 여러 개 만들 수 있게 하기: Certificate, Projects, Skills 등
  - 이름이 정해진 기본 섹션을 복제하는 것이 아니라, 템플릿 컴포넌트를 복제함. 사용자는 경력 작성형, 텍스트 작성형, 이미지 나열형 템플릿 중 하나를 선택할 수 있게 됨.
- [x] 영어로 번역되지 않은 중국어 UI를 모두 영어로 번역
  - 커스텀 섹션 추가 시 UI가 중국어로 나옴
  - 일부 섹션에서의 텍스트 입력란 헬프텍스트가 중국어로 나옴
  - AI 프롬프트를 영어로 번역하고, 언어 중립적 표현으로 수정함
- [x] 언어 기본값 영어로 설정
- [x] 한국어 버전 UI 추가하기
- [x] 메인 홈 화면뿐만 아니라 다른 곳에서도 언어 변경이 가능하게 하기.