services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: deps
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: "true"
    command: sh -c "if [ ! -x node_modules/.bin/vite ]; then pnpm install --frozen-lockfile; fi && pnpm dev --host 0.0.0.0 --port 3000"
    volumes:
      - .:/app
      - web_node_modules:/app/node_modules
    ports:
      - "3000:3000"

volumes:
  web_node_modules:
